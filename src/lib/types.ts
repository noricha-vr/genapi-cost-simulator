export type ModelData = {
    name: string;
    inputCost: number;
    outputCost: number;
    active: boolean;
};

export type Currency = {
    code: string;
    rate: number;
    symbol: string;
};
